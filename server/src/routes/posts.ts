import { Router } from "express";

const postRouter = Router();

// TODO: Add routes here

// GET request that gets 10 posts paginated in order of most recent
// postRouter.get("/", async (req, res) => {

export default postRouter;
